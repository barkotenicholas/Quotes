import { MostvotesDirective } from './mostvotes.directive';

describe('MostvotesDirective', () => {
  it('should create an instance', () => {
    const directive = new MostvotesDirective();
    expect(directive).toBeTruthy();
  });
});
